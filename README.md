# Rotiser-a_Pedidos
# Rotisería - Pedidos (Streamlit + Google Sheets)

## Run local
1) python -m venv .venv
2) activate venv
3) pip install -r requirements.txt
4) Configure .streamlit/secrets.toml
5) streamlit run src/app.py
